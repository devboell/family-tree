import styled from 'styled-components'

const CheckboxWrapper = styled.div`

  margin-right: 10px;
  > label {
    font-weight: bold;
    margin-left: 5px;
    vertical-align: -1px;
  }
`

export default CheckboxWrapper
