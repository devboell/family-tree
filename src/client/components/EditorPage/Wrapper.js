import styled from 'styled-components'
import { pageBlock } from 'globalStyles'


const Wrapper = styled.div`
  ${pageBlock}
  color: white;
  background-color: rgb(64,64,64);
`

export default Wrapper
